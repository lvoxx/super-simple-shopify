<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.platform.persistence.ShopShardAssignmentMapper">

	<insert id="insert">
		INSERT INTO shop_shard_assignment (shop_id, shard_index, assigned_at)
		VALUES (#{shopId}, #{shardIndex}, #{assignedAt})
	</insert>

	<select id="findShard" resultType="java.lang.Integer">
		SELECT shard_index FROM shop_shard_assignment WHERE shop_id = #{shopId}
	</select>

</mapper>
